# Discovery keybinds

> Sas de décision — les découvertes attendent ici leur statut. L'adoptée migre
> vers la File priorisée du roadmap ; l'écartée reste en mémoire.

| ID | Date | Découverte | Constat / preuve | Reco | Statut | Décision |
|---|---|---|---|---|---|---|
| F-01 | 10/09 | Ctrl+T natif scrolle en bas au hide | Constat terrain : toggle thinking ok à chaud, mais « ça me descend tt en bas de la discussion » à l'enclenchement — casse la navigation | Le toggle futur doit préserver la position de lecture du transcript (scroll anchor) | ✅ | Livré v1.1 — anchor OSC-133 (capture → renderNow → restore) |
| F-02 | 10/09 | Ctrl+O natif quasi sans effet | Constat terrain : « ne change quasiment rien, c'est pas très utile » | Ne pas bâtir la vue épurée dessus ; passer par `setToolsExpanded` + renderers | ✅ | Décision appliquée — setToolsExpanded écarté, masquage dur par override render instance (v1.1) |
| F-03 | 10/09 | Objectif réel = navigation rapide | « remonter à un msg, réafficher les trucs » — la vue épurée n'est qu'un moyen | Cahier des charges du point 1 du roadmap : masquer + retrouver + réafficher | ✅ | Livré v1.1 — toggle F5 + navigation native Ctrl+↑/↓ (marques OSC-133 user/assistant only) |
| F-04 | 10/09 | La vue « que nos messages » existe côté /tree | keybindings.md : `app.tree.filter.userOnly` (Ctrl+U) / `noTools` (Ctrl+T) ; `treeFilterMode` est persisté dans settings.json | Source d'inspiration UX : un mode persisté pour le toggle ? | 🆕 | — |
| F-05 | 10/09 | Listener Ctrl+C + molette perdus après un switch tui-mode à chaud | `switchTuiMode` (interactive-mode.js:549) recrée la TuiAltScreen ; le WeakSet `installedListeners` de keybinds.ts est indexé sur l'ANCIENNE tui → la nouvelle n'a ni listener Ctrl+C ni molette ×4 | Résoudre les refs TUI en lazy à chaque usage (déjà fait pour le toggle épuré) ; backporter le fix au listener Ctrl+C | 🆕 | — |
| F-06 | 12/09 | Texte long dans l'éditeur : impossible d'étendre une sélection au-delà du viewport | Terrain : sélection drag ne peut pas sortir du champ ; pi exclut l'éditeur de son autoscroll (réservé aux ScrollViews) et son render re-clampe scrollOffset sur le curseur. v1.4-écartée (wrappers de méthodes TuiAltScreen) muette en réel, cause non diagnostiquée. v1.4-flèches retirée : débordements de peinture (bornes hors box → peinture native sur transcript/sous le champ) | Socle validé en réel : branchement post-render (override render MyPiEditor) + champs alt. Design : bornes TOUJOURS clampées dans la box (jamais de row hors écran) + offsets figés pour la conversion ; timer 50 ms LAST RESORT piloté depuis le render (légitimé par le natif pi) pour le drag-aux-bords ; molette pendant drag via addInputListener (mécanisme validé v1.0). Primitives en labo : dev/lab-arrows.mjs 6/6, dev/lab-autoscroll.mjs 5/5 | 💬 | — |
| F-07 | 13/09 | Paste (bracketed paste) ne remplace pas la sélection écran | GUI standard : coller remplace la sélection. Ici la séquence `\x1b[200~…` est exclue du remplacement (contient ESC) → la sélection reste et le collage s'insère au curseur devant elle | Si voulu : détecter le préfixe bracketed paste dans replacesScreenSelection et supprimer la sélection avant délégation (le contenu colle à sa place) | 🆕 | — |
| F-08 | 13/09 | Les flèches ne vident pas la sélection écran | GUI standard : déplacer le curseur désélectionne. Ici la sélection écran reste surlignée après un mouvement (pi ne connaît pas la sélection écran) | Si voulu : sur touches curseur avec sélection éditeur active, appeler clearTextSelection avant délégation | 🆕 | — |
| F-09 | 14/09 | Pas de « tout sélectionner » dans le champ de saisie | Vérifié code + doc : aucun binding select-all (Ctrl+A natif pi = début/fin de ligne dans l'éditeur, filtre « tout » dans la palette). Demande Impre : sélectionner le contenu du champ en une fois — transcript EXCLU du périmètre (le drag souris le couvre déjà) | Ctrl+A dans l'éditeur = sélectionner tout le contenu du champ (convention GUI). Mécanisme candidat : poser la sélection écran sur les rows de l'éditeur — l'inverse de la conversion v1.3 (on sait lire la sélection, reste à la créer par code) ; à vérifier : API pi-tui pour poser une sélection, et le remap du Ctrl+A natif (beginning-of-line) qui perdrait son rôle | ✅ | Livré v1.5 — pose directe selectionAnchor/selectionFocus ; trade-off début-de-ligne assumé (Home conservé) |
